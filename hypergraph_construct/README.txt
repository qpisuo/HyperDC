hypergraph_construct：combine single drug and combination data to construct the hypergraph

1. final hypergraph:
hypergraph_index.csv: hypergraph with nodes represented by index in primekg
hypergraph_name.csv: hypergraph with nodes represented by name (drug name: drugbank id; disease name: node name in primekg)

2. other files:
nodes.csv: all nodes in primekg
node_idx_map.csv: all drug and disease nodes in hypergraph